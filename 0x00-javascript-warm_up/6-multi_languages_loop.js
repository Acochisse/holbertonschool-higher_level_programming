#!/usr/bin/node
// script that prints a loop of lines

const line = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let step = 0; step < line.length; step++) {
  console.log(line[step]);
}